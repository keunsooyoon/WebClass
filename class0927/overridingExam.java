package class0927;

public class overridingExam {
    /*
    부모 클래스로부터 상속받은 매서드의 내용을 변경하는 것을 오버라이딩이라 한다. 
    상속 받은 매서드를 그대로 사용하지만 자식 클래스가 자신에 맞게 변경해야 하는 경우가 많다. 
    매서드 오버라이딩...

    class Point {
        int x;
        int y;

        int getNum() {
            return x + y;
        }
    }

    class Point3D extends Point{
        int getNum() {
            return x - y;
        }

    }
    오버라이딩의 조건
    1. 이름이 같아야 한다. 
    2. 매개변수가 같아야 한다. 
    3. 반환타입이 같아야 한다. 
    *. 접근 제어자 등 제한된 조건 하에서만 다르게 변경할 수 있다. 


    오버로딩 - 기존에 없는 새로운 여러개 매서드를 정의하는 것 
    오버라이딩- 상속받은, 기존에 있는 매서드의 내용을 변경하는 것



    */
}
